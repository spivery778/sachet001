name: sachet
description: "An app designed for Xiangtan University students, offering easy-to-use course schedules, convenient free-classrooms search, clear exam information, and other features."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.7.2+12

homepage: https://github.com/wyvern1723/sachet
repository: https://github.com/wyvern1723/sachet

environment:
  sdk: '>=3.2.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 👇 核心插件（已锁死版本，去掉了 ^，确保兼容 Dart 3.3.4）
  shared_preferences: 2.2.2          # 降级锁死
  flutter_local_notifications: 17.0.0 # 我们刚才改好的
  flutter_localizations:
    sdk: flutter
  
  # 👇 界面图标
  cupertino_icons: ^1.0.2

  # 👇 必须锁死的"捣乱分子" (防止它们自动升级)
  path_provider: 2.1.2               # 修复你现在的报错
  file_picker: 6.1.1                 # 保持不动
  flutter_secure_storage: 9.0.0      # 保持不动
  path: 1.9.0
  
  # 状态管理
  provider: 6.1.1                    # 稍微降一点比较稳
  
  # 网络
  http: ^1.2.0
  html: ^0.15.4
  dio: 5.4.0                         # 锁一下比较安全
  flutter_inappwebview: 6.0.0        # 这里的 6.1.x 可能会报错，建议锁死 6.0.0
  
  # AI与图像
  tflite_flutter: ^0.11.0
  image: ^4.1.0

  # 系统功能 (这些最容易报错，必须锁死！)
  url_launcher: 6.2.5                # ⚠️ 预判：不锁这个，下一个必炸
  flutter_native_splash: 2.3.10      # 稍微降级求稳
  flutter_launcher_icons: ^0.13.1
  flutter_colorpicker: ^1.1.0
  
  package_info_plus: 5.0.1           # ⚠️ 预判：8.0以上必炸，强行降级
  flutter_markdown: 0.7.2            # ⚠️ 预判：0.7.5以上必炸
  dynamic_color: 1.7.0
  pointycastle: ^3.7.3               # 你的截图里是 3.7.4+，如果报错改回这个

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

# app icon (应用图标)
# 生成有关应用图标的文件
# dart run flutter_launcher_icons
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_foreground: "assets/icon/icon-fg.png"
  adaptive_icon_background: "assets/icon/icon-bg.png"
  adaptive_icon_monochrome: "assets/icon/icon-monochrome.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  windows:
    generate: true
    image_path: "assets/icon/icon.png"
    icon_size: 256 # min:48, max:256, default: 48

# splash screen (应用启动页面)
# 生成有关 splash screen 的文件
# dart run flutter_native_splash:create
flutter_native_splash:
  image: assets/icon/icon-fg_1152x1152.png
  color: "#a5d6a7"
  color_dark: "#000000"
  fullscreen: true # 需要设为 true，否则会显示系统的状态栏
  android_12:
    color: "#a5d6a7"
    color_dark: "#000000"

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icon/
    - assets/json/
    - assets/json/ClassSessionSummerRoutine.json  # 夏令时上课时间
    - assets/json/ClassSessionWinterRoutine.json  # 冬令时上课时间
    - assets/models/captcha_model.tflite          # 自动识别图片验证码的 tflite 模型(使用 TensorFlow v2.15.0 训练生成)
